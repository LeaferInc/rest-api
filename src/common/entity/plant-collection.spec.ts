import { PlantCollectionEntity } from './plant-collection.entity';

describe('PlantCollection', () => {
  it('should be defined', () => {
    expect(new PlantCollectionEntity()).toBeDefined();
  });
});
